# Sobre os dados

O app roda com os **dados reais** cruzados a partir de:
- IBGE — MUNIC, Suplemento de Cultura **2021** (equipamentos culturais)
- IBGE — Censo Demográfico **2022** (população e renda per capita)

O arquivo final está em `data/radar_cultural_ce.csv`, com os 184 municípios
do Ceará e as colunas:

| Coluna | Descrição |
|---|---|
| `municipio` | Nome do município |
| `codigo_ibge` | Código IBGE de 7 dígitos |
| `mesorregiao` | Uma das 7 mesorregiões do Ceará |
| `populacao` | População (Censo 2022, resultado do Universo — definitivo) |
| `renda_per_capita` | Renda per capita em R$ (Censo 2022, resultado preliminar da amostra) |
| `lat` / `lon` | Coordenadas do município (centróide), usadas no mapa interativo e no cálculo de distância do Simulador |
| `tem_museu` / `tem_teatro_sala_espetaculo` / `tem_cinema` / `tem_biblioteca` | Existência de cada equipamento (MUNIC 2021) |
| `n_equipamentos` | Soma dos 4 equipamentos acima (0 a 4) — recalculada em runtime por `data_loader.py`, a coluna do CSV não é usada diretamente |

**Achado relevante:** biblioteca pública existe em **99,5% dos municípios
do Ceará** (183 de 184 — só Graça é exceção), então ela quase não ajuda a
diferenciar quem tem acesso cultural de quem não tem. Por isso os KPIs e
o Índice de Prioridade do app usam apenas museu + teatro + cinema como
equipamentos "raros" — vale citar esse achado no relatório do desafio, é
um ponto forte para o critério de tratamento de dados.

## Como os dados foram cruzados

1. **MUNIC 2021**, aba "Cultura" da base de dados completa (`Base_MUNIC_
   2021_20240425.xlsx`), seção "5.7 Equipamentos" — variáveis `MCUL3901`
   (Bibliotecas públicas), `MCUL3902` (Museus), `MCUL3903` (Teatros ou
   salas de espetáculos) e `MCUL3909` (Cinema), todas de "existência" no
   município (não "quem administra"). Filtrado pelo código IBGE
   começando em `23` (Ceará) → 184 municípios. Diferente de 2014, essa
   aba já traz `CodMun`/`Mun`/`Pop` direto, sem precisar cruzar com a
   aba "Variáveis externas".
2. **População (Censo 2022)** — tabela SIDRA **4714** ("População
   Residente, Área territorial e Densidade demográfica"), variável
   "População residente", nível município, todos os 5.570 municípios do
   Brasil, filtrado pelo sufixo `(CE)` no nome → 184 municípios do Ceará.
   É o resultado do **Universo** (definitivo, não é amostra).
3. **Renda per capita (Censo 2022)** — tabela SIDRA **10295** ("Valor do
   rendimento nominal médio mensal domiciliar per capita..."), mesmo
   processo de filtro. É resultado **preliminar da amostra** — o IBGE
   ainda não fechou as áreas de ponderação definitivas pra esse indicador,
   mas é o dado municipal mais atual disponível hoje.
4. **Correção de grafia**: dois municípios têm o nome escrito de forma
   diferente entre a base do IBGE (Censo 2022 e MUNIC 2021) e a base já
   usada no projeto — `Ereré`→`Ererê` e `Itapajé`→`Itapagé`. Foram
   renomeados pra bater com o nome já usado no dataset, pra não perder o
   cruzamento nesses dois municípios (mesma dupla de nomes que já
   precisou de correção quando cruzamos o Censo 2022 pela primeira vez).
5. **Mesorregião**: cruzada pelo código IBGE do município usando a
   classificação oficial de mesorregiões do IBGE (código de 4 dígitos:
   2 primeiros = UF, 2 seguintes = mesorregião).
6. **Coordenadas (`lat`/`lon`)**: cruzadas de uma base pública de
   municípios brasileiros pelo código IBGE, usadas pro mapa interativo
   (`src/charts.py`) e pro cálculo de distância real do Simulador de
   Investimento (fórmula de Haversine, `src/geo.py`). Zero municípios
   ficaram sem coordenada.

Todos os cruzamentos bateram **184 de 184** municípios, sem nenhuma linha
perdida em nenhuma das etapas.

## Por que MUNIC 2021 (e não 2014, como antes)

A base original do projeto usava a MUNIC 2014 pra equipamentos culturais
— era o Suplemento de Cultura mais recente na época em que o projeto
começou. A MUNIC 2021 é o próximo Suplemento de Cultura disponível; é a
edição mais atual do IBGE com esse nível de detalhe.

**Fontes alternativas que consideramos e descartamos:**
- **Rede Pública de Espaços e Equipamentos Culturais do Ceará (Rece)** —
  descartada porque só lista os ~27-28 equipamentos mantidos
  **diretamente pela Secult**, não todo museu/teatro/cinema do estado
  (municipal, privado, etc.). Usar essa fonte inflaria artificialmente o
  número de "desertos culturais".
- **Mapa Cultural do Ceará** (`mapacultural.secult.ce.gov.br`) — tem API
  real e documentada, mas é uma plataforma **colaborativa** (qualquer
  agente cultural cadastra), o que gera risco de cobertura desigual
  entre municípios — diferente da MUNIC, que é um censo estruturado
  respondido pela prefeitura de cada município. Ficou registrada como
  opção pra uma eventual validação cruzada no futuro, mas não como fonte
  principal.

Com essa troca, o "gap" entre o ano da MUNIC e o ano do Censo caiu de 4
anos (2014 vs 2010) para **apenas 1 ano** (2021 vs 2022) — o conjunto de
dados do projeto ficou bem mais contemporâneo.

## Demanda Cidadã 

O segundo modo do app ("Demanda Cidadã") é uma ferramenta de coleta e demanda: o
cidadão escolhe o município dele, vê o que já existe (dado real do Radar
Cultural) e registra — entre o que falta — o que mais gostaria de ver.
Inclui duas categorias "aspiracionais" (Centro Cultural, Oficina
Itinerante Cultural) que não têm coluna no dataset real, então sempre
aparecem como opção de pedido, em qualquer município.

Isso se conecta com o resto do app de duas formas, ambas em
`src/demanda.py`:

1. **Índice de Prioridade ajustado**: cada pedido soma um peso pequeno e
   suave (raiz quadrada do total de pedidos × 0,05) ao Índice de
   Prioridade do município — visível na página "Municípios
   Prioritários" do Painel do Gestor, numa coluna separada (o índice
   original, calculado só com dado real, nunca é sobrescrito).
2. **Sugestão no Simulador**: ao clicar num município que já tem pedidos
   registrados, o Simulador de Investimento sugere automaticamente
   simular o tipo de equipamento mais pedido pela população dali (quando
   esse tipo for simulável — museu, teatro ou cinema; biblioteca e as
   categorias aspiracionais ainda não entram no Simulador).

**Mural de feedback**: além do pedido em si, cada município tem um mural
de comentários (apelido + avatar/foto + texto livre + like/dislike +
resposta), pensado como espaço pra contexto que um voto sozinho não
carrega ("por que esse equipamento importa aqui"). Mesmo componente
reaproveitado nas 3 seções que mostram feedback (Demanda Cidadã, Painel
do Gestor, Simulador & Transparência). Um voto (like ou dislike) por
sessão por comentário; clicar de novo desfaz.

Persistência: sessão do navegador, como o resto do protótipo — os
pedidos e o mural somem ao recarregar a página. Os arquivos antigos do
Feed (`src/eventos.py`, `src/feed_ui.py`) não são mais usados pelo
`streamlit_app.py` e podem ser apagados do repositório.

## Painel de Transparência do Gestor (Simulador & Transparência)

O terceiro modo do app ("Simulador & Transparência") tem duas partes:

1. **Simulador de Investimento** — mapa clicável, calcula com dados 100%
   reais (mesmas coordenadas e fórmula de distância do resto do app) o
   impacto de instalar um equipamento cultural num município.
2. **Painel de Transparência** — versão pública do Índice de Prioridade
   (o mesmo já usado em "Municípios Prioritários"), com cards em
   formato retrato prontos pra baixar e compartilhar em redes sociais —
   pensado como ferramenta de cobrança cívica, não só de planejamento
   interno. Gerado com Pillow (`src/transparencia.py`), sem depender de
   nenhuma fonte de dado externa.

**Nota:** cogitamos incluir também um "Termômetro de Investimento
Público" cruzando o Índice de Prioridade com investimento público real
(via SALIC/Lei Rouanet) — a API existe e é pública, mas o formato dela
(sem filtro confiável por UF, registros de projeto enormes) tornou a
extração inviável dentro do tempo do desafio. Decidimos substituir por
este Painel de Transparência, que já entrega o mesmo tipo de pressão de
accountability usando só dado que já tínhamos validado.

## Gamificação (perfil, progresso de exploração e conquistas)

Módulo `src/gamificacao.py`. Três mecânicas, todas session-based (zeram
ao recarregar a página, sem banco de dados nem login):

1. **Perfil**: apelido + avatar (emoji ou foto recortada/comprimida em
   base64 com Pillow) — dá identidade ao mural de feedback e ao "Meu
   Perfil", mas não é autenticação de verdade.
2. **Progresso de exploração**: conta quantos dos 184 municípios a
   pessoa já "visitou" — abrindo na Demanda Cidadã ou clicando no mapa
   do Simulador.
3. **Conquistas**: 6 badges desbloqueados por uso — Primeiro Pedido,
   Explorador (5 municípios), Voz Ativa (3 mesorregiões), Cartógrafo (20
   municípios), Atento ao Deserto (pedido registrado num Deserto
   Cultural) e Mobilizador (10 pedidos).

## Acessibilidade a cores (daltonismo)

Módulo `src/acessibilidade.py` (controles) + `src/theme.py` (paletas).
As cores que **diferenciam categorias na mesma tela** — as 3 cores das
features, o par "Tem"/"Deserto Cultural" do mapa do Simulador, e a
paleta discreta/sequencial dos gráficos (incluindo as 7 mesorregiões
listadas na seção "Como os dados foram cruzados") — trocam de conjunto
conforme o modo escolhido: Protanopia/Protanomalia, Deuteranopia/
Deuteranomalia ou Tritanopia/Tritanomalia. Anopia (ausência total) e
anomalia (percepção reduzida) do mesmo eixo de cor usam o mesmo ajuste.

As paletas protan/deutan partem da **Okabe–Ito** (Color Universal
Design), uma referência com segurança documentada pros dois tipos de
daltonismo vermelho-verde ao mesmo tempo. A paleta tritan evita os pares
azul/verde e amarelo/violeta (os que se confundem nesse eixo) e usa a
distinção vermelho-verde, que segue intacta na tritanopia. Cores usadas
sozinhas (o terracota do cabeçalho, por exemplo) não mudam — não há
ambiguidade a resolver quando só existe uma cor na tela.